package kappa.coordinator.test

import kappa._
import kappa.Directives._
import kappa.coordinator._

/**
  * Created by j0rd1 on 6/11/16.
  */
object Test {
/*
  def main(args: Array[String]) {
    val dir = start()
    val response = dir.execute()
    println(response)
  }

  def start() = {
    executeCommand("My command") {
      response("ok")
    } failure { t ⇒
      response(t.getLocalizedMessage)
    }
  }
*/
}
